<form method="post" action="{{ path('program_delete', {'program_slug': program.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this program ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
    <button class="btn">Delete</button>
</form>
