<sidebar class="d-none d-md-flex flex-column flex-shrink-0 col-3 col-lg-2 min-vh-100 border-secondary">
    <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-dark text-decoration-none">
        <span class="fs-4">Series</span>
    </a>
    <hr>
    <ul class="nav flex-column mb-auto">
        {%  for category in categories %}
            <li>
                <a href=" {{ path('category_index') }}{{category.name}}" class="nav-link active" aria-current="page">{{category.name}}</a>
            </li>
        {% endfor %}
        <li>
            <a href="#" class="nav-link link-dark">Customers</a>
        </li>
    </ul>
</sidebar>